# Rectangle Area Calculate
# A = wl (where A = Area, w = width and l = length)

length = float(input("Enter the length: "))
width = float(input("Enter the width: "))
print(f"Area of the rectangle: {length * width}cm²")